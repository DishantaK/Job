import React from 'react';


const LocationFilter = () => (
  <div className="location filterBox">
    <h6 >  Location </h6>
    {/* Add header */}
    {/* Required fields from model.  
      1. Conditional Render - > Remote or Physical
     2. If physical, display location input - if remote, search by remote keep default render
    */}
  </div>

)

export default LocationFilter;